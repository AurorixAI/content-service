# EVAL — метрики качества оцифровки

Каждая строка дописывается `scripts/eval.py`. Метрики — `src/eval/metrics.py`.
`—` = метрика недоступна на этом этапе (**не путать с измеренным нулём**).

```bash
python3 scripts/eval.py --class-level 8
docker exec content-worker python3 /app/scripts/eval.py --class-level 7
```

| Дата | Класс | task_recall | compile_rate | numbering_gaps | latex_ned | answer_coverage | n_tasks | Примечание |
|------|-------|-------------|--------------|----------------|-----------|-----------------|---------|------------|
| 2026-08-28 | — | — | 0.9989 | — | — | 0.0411 | 2 603 | Сквозной прогон обновлённого контура, 6 книг прототипа |
| 2026-08-30 | — | — | 0.9992 | — | — | 0.0411 | 2 603 | С4+С5: структурный слой, скоринг доверия, B1 закрыт |
| 2026-08-30 | 5 | — | **1.0000** | — | — | **0.6128** | **687** | **Первый живой прогон реальной книги** (textzadachi5, Gemini API-ключ) |

<!-- Строк по tasks_master с данными G7/G8 пока нет: нужен дамп со staging.
     task_recall и latex_ned требуют вручную вычитанного golden
     (src/eval/golden/README.md) — он не засеян. -->

**Как прочитана строка 2026-08-28.** Источник — выгрузка прототипа, прогнанная
через новый контур (`scripts/demo_pipeline.py`), а не `tasks_master` с G7/G8.
`compile_rate` считается по формулам, выдранным из `statement_md` (3 781 формула);
в замере С2 их было 10 769, потому что там сканировались все поля записи, включая
подпункты — цифры не сравнимы напрямую, сравнимо только число битых: **4 там и 4
здесь**. `answer_coverage 0.0411` — это 107 ответов из книги на 2 603 задачи, и
низкая она не из-за дефекта: раздел «Ответы» переизвлечён ровно у одной книги
из шести.

---

## Сквозной прогон обновлённого контура

*2026-08-28. `python3 scripts/demo_pipeline.py --apply`, 6 книг, без вызовов API.*

| Книга | Задач | Ответы книги | Формул | Битых | Артеф. | pass | review | reject |
|---|---|---|---|---|---|---|---|---|
| akhtamov4 | 41 | 0/0 | 142 | 0 | 0 | 0 | 41 | 0 |
| algebra79 | 314 | 0/0 | 76 | 0 | 0 | 0 | 230 | 84 |
| bogomolov | 520 | 0/0 | 661 | 0 | 0 | 0 | 430 | 90 |
| textzadachi5 | 590 | **107/109** | 663 | 0 | 0 | 75 | 328 | 187 |
| usmanov2 | 407 | 0/0 | 665 | 1 | 1 | 0 | 399 | 8 |
| ДТМ2020 | 731 | 0/0 | 1 574 | 3 | 12 | 0 | 731 | 0 |
| **Итого** | **2 603** | **107/109** | **3 781** | **4** | **13** | **75** | **2 159** | **369** |

### Что эти цифры показывают

1. **107 из 109 книжных ответов пришиты.** Они лежали в
   `newocr/mathocr/data/out/textzadachi5/answers.json` с июля и не были
   использованы ни разу: у прототипа join не запускался, у content-service
   раздела «Ответы» не было вовсе. Два непришитых — номера, которых нет среди
   задач (дефект сегментации, виден в `unmatched_answers`).
2. **2 496 задач остались без книжного ответа.** Старый контур выдумал бы их все
   через `AIAnswerSolver` и записал в то же поле, что и книжные, без пометки.
   Новый ставит `answer_source` и не пускает такие в `tasks_master` без человека.
3. **Артефактов (13) больше, чем непроходящих компиляцию (4)** — вывод С2
   воспроизвёлся на живом прогоне. `rac{1}{2}` компилируется без ошибки и
   показывает мусор; его ловит только лексический детектор.
4. **369 отказов — это `reject`, а не потеря.** 317 задач с пустым условием и
   52 обрывка короче 12 символов. Раньше `passes_quality_gate` отбрасывал их
   молча и без причины; теперь причина записана в `gate_reasons`.
5. **Дубли идентификаторов: 22 задачи.** `task_id` в выгрузке прототипа не
   уникален (usmanov2 — 11 пар, bogomolov — 2). При записи они конфликтовали
   по ключу и исчезали: 2 603 на входе, 2 592 в базе. Теперь идентификаторы
   разводятся, а сам дефект помечается гейтом.

### Выбор ключа join — почему у четырёх книг он отказал

| Книга | Ключ | Пришито | Почему |
|---|---|---|---|
| akhtamov4 | paragraph_number | 0 | номера массово повторяются |
| algebra79 | paragraph_number | 0 | номера массово повторяются |
| bogomolov | number | 0 | нет `answers.json`; 25 номеров отказано поштучно |
| textzadachi5 | number | 107 | сквозная нумерация; 51 номер отказан поштучно |
| usmanov2 | paragraph_number | 0 | номера массово повторяются |
| ДТМ2020 | paragraph_number | 0 | **731 задача на 110 уникальных номеров** |

ДТМ2020 — показательный случай: номер «1» встречается 16 раз. Join по одному
номеру раздал бы ответы наугад, и результат выглядел бы как высокое покрытие.
Система отказывается вместо того, чтобы угадать, — и это отказ по замеру,
а не по настройке (`answer_key.DUPLICATE_NUMBER_THRESHOLD`).

---

## Качество LaTeX по книгам (источник — выгрузка прототипа, не БД)

Прогон `scripts/audit_latex_jsonl.py` по `newocr/mathocr/data/out/*/tasks.json`,
2026-08-23. Golden не нужен — метрика не сравнительная.
**6 книг, 2 654 задачи, 10 769 формул.**

| Книга | Задач | Формул | Не компилируется | compile_rate | Артефактов | Чинится ремонтом |
|---|---|---|---|---|---|---|
| ДТМ2020 | 731 | 3 662 | 3 | 0.9992 | 14 | 3/3 |
| usmanov2 | 407 | 2 058 | 1 | 0.9995 | 1 | 0/1 |
| bogomolov | 571 | 2 241 | 0 | 1.0000 | 0 | — |
| textzadachi5 | 590 | 1 312 | 0 | 1.0000 | 0 | — |
| algebra79 | 314 | 1 211 | 0 | 1.0000 | 0 | — |
| akhtamov4 | 41 | 285 | 0 | 1.0000 | 0 | — |
| **Итого** | **2 654** | **10 769** | **4** | **0.9996** | **15** | **3/4** |

### Что из этого следует

1. **Артефактов (15) больше, чем непроходящих компиляцию (4).** 11 формул
   рендерятся без ошибки, но показывают мусор (`\\text{ cm}` → «text{ cm}»).
   Один `compile_rate` их не видит — подтверждение, зачем нужен второй детектор.
2. **Дефект не размазан, а сидит на одной странице.** 13 из 14 артефактов
   ДТМ2020 — стр. 88 (30% формул страницы). Это отказ обработки одного ответа
   модели, а не постепенный шум OCR.
3. **Корень — экранирование, не распознавание.** В `usmanov2` `\frac` записан
   как `0x0c` (form feed) + `rac`: escape-последовательность `\f` была съедена,
   а соседний `\left` уцелел (`\l` — не escape). Чинить в слое разбора ответа
   модели; ремонт скриптом лечит следствие в уже накопленных данных.

### Уточнение по факту (2026-08-25): это не один класс, а два

Пересчёт управляющих символов по всей выгрузке (27 297 строк) развёл то, что
раньше считалось одним корнем.

| Класс | Признак | Сколько | Корень |
|---|---|---|---|
| Съеденный escape | управляющий символ внутри формулы | 3 | слой разбора ответа модели |
| Задвоенный слэш | `\\text` перед именем команды | 18 (все ДТМ2020) | модель экранирует дважды |

**`ight)` — не потерянный слэш и не артефакт OCR.** В ДТМ2020 это `0x0d` (CR):
`\right` съеден как `\r` — ровно тот же механизм, что и `\frac` через `\f`.
Значит **B5 и B11 — один корень**, а не два разных бага. Тем же механизмом
портятся `\text`/`\times`/`\theta`/`\to` (таб), `\begin` (backspace), `\neq`
(перевод строки) — класс шире, чем считалось.

**Задвоенный слэш — корень другой.** Проверено на исправленном парсере: на входе
`"2\\\\text{ cm}"` он отдаёт `2\\text{ cm}` — это корректное поведение JSON,
два слэша прислала сама модель. Разбором не чинится: нужен гейт
`latex_artifacts` на шве либо правка промпта.

---

## Сессии 4–5: структурный слой и очередь проверки

*2026-08-30. `python3 scripts/demo_pipeline.py`, те же 6 книг, без вызовов API —
поэтому цифры сравнимы со строкой 2026-08-28 напрямую.*

| Показатель | Было (С3) | Стало (С5) | Что произошло |
|---|---|---|---|
| Формул проверено | 3 781 | **5 209** | +1 428: вернулся текст подпунктов, который терялся при приёме |
| Битых формул | 4 | 4 | не изменилось |
| `compile_rate` | 0.9989 | **0.9992** | вырос за счёт знаменателя, а не ремонта |
| Вердикт `reject` | 369 | **52** | −317 ложных отказов (B1) |
| Вердикт `pass` | 75 | **107** | все — `textzadachi5` с ответами из книги |
| Очередь ручной проверки | 97.2% | **3.2%** | сигнал качества отделён от сигнала полноты |

### B1 закрыт, и он оказался не там, где числился

В реестре B1 стоял как дефект скоринга (`mathocr/validate/score.py:74`) —
«ложный `needs_review` при пустом условии». По факту корень был в двух других
местах, и цена была выше, чем считалось:

1. **Приём терял содержание.** `prototype_ingest` переносил подпункты только у
   MCQ. У задач формата «1. а) … б) …» весь текст лежит в подпунктах — он
   выбрасывался, и задача приезжала пустой. Это 185 задач из 590 на
   `textzadachi5` и 1 428 формул по корпусу.
2. **Гейт отвергал их насовсем.** `check_structure` ставил `reject` за пустое
   условие, не глядя на подпункты. То есть треть книги не «попадала в очередь
   проверки», а **не попадала в банк вообще**.

Скоринг — третье место, где то же условие проверялось повторно; там правка
тоже сделана.

### Почему очередь упала с 97.2% до 3.2%

Первый замер после переноса дал 97.2% — то есть «на проверку» уходила почти вся
книга. Причина не в порогах: 2 496 задач из 2 603 помечались единственной
причиной «нет ответа». У выгрузки прототипа раздел «Ответы» переизвлечён у одной
книги из шести — это свойство **корпуса**, а не качества распознавания.

Смешивать это с браком нельзя: очередь, в которую попало 97% книги, ничего не
приоритизирует. Сигналы разведены:

* `needs_review` — **брак**: битая формула, лексический артефакт, структурное
  повреждение, разрыв нумерации, жёсткий отказ гейта;
* `awaiting_answer` — **полнота**: ответа из книги нет.

Это не смягчение гейта. Условие промоушена не менялось: в `tasks_master`
по-прежнему уходит только `gate_status = pass`, и задача без ответа туда не
попадёт независимо от того, в какой она очереди.

| Книга | Брак | % книги | Ждут ответа |
|---|---|---|---|
| akhtamov4 | 0 | 0.0% | 41 |
| algebra79 | 27 | 8.6% | 314 |
| bogomolov | 21 | 4.0% | 520 |
| textzadachi5 | 18 | 3.1% | 483 |
| usmanov2 | 12 | 2.9% | 407 |
| ДТМ2020 | 6 | 0.8% | 731 |
| **Итого** | **84** | **3.2%** | **2 496** |

### Структурный слой — что он поправил

| Книга | Ложный контекст снят | Контекст распространён | Склеено через разрыв | Разделов пересортировано |
|---|---|---|---|---|
| akhtamov4 | 0 | 0 | 0 | 1 |
| algebra79 | 6 | 0 | 0 | 0 |
| bogomolov | 0 | 0 | 0 | 3 |
| textzadachi5 | 0 | 44 | 0 | 5 |
| usmanov2 | 38 | 0 | 0 | 2 |
| ДТМ2020 | 0 | 0 | 0 | 1 |
| **Итого** | **44** | **44** | **0** | **12** |

**Ноль склеек — не отказ модуля.** Склейка требует фрагмента без номера, а
извлечение прототипа проставило номер практически везде. Модуль покрыт
синтетическими тестами (`tests/test_structure.py`), а его настоящая проверка —
первый прогон реальной книги через OCR, где разрывы страниц живые.

---

---

## Первый живой прогон реальной книги

*2026-08-30. «Текстовые задачи по математике, 5 класс», 108 стр., 23 листовых
параграфа. Gemini 3.5 Flash по API-ключу. Единственный прогон, где конвейер
работал целиком: PDF → OCR → извлечение → структура → гейты → скоринг →
`tasks_staging`.*

| Показатель | Значение |
|---|---|
| Задач извлечено | **687** из 23 параграфов |
| **Ответы из книги** (`book_key`) | **421 (61.3%)** |
| Ответы модели (`ai_solved`) | 266 |
| Без ответа (`absent`) | **0** |
| `compile_rate` | **1.0000** (1 679 формул) |
| Лексических артефактов LaTeX | **0** |
| Брак распознавания (`needs_review`) | **0** |
| Дистракторы ≥2 | 647 (94.2%) |
| Вердикты | pass **421** / review 266 / reject **0** |
| Промоушен в `tasks_master` | **0 — остановлен заслоном** |

### Раздел «Ответы» разобран без единого вызова API

427 ответов с 11 страниц взяты из **текстового слоя PDF**. У книги-скана с
распознаванием он есть, и он точнее модели на печатном списке. Vision остаётся
фолбэком для книг без слоя.

Это и есть источник цифры 61.3%: 421 задача получила ответ, **напечатанный в
учебнике**, а не выведенный моделью. Прежний контур выдумал бы все 687 и записал
их неотличимо от книжных.

### Промоушен остановился сам, и это правильно

```
промоушен: все 421 задач замаплены на один навык DEMO_S01_01_01 —
похоже, граф знаний пуст. Промоушен остановлен.
```

Локально `knowledge_hierarchy` — 4 узла-заглушки, классификатору не из чего
выбирать. Заслон (B21) добавлен в этом же заходе, после того как выяснилось,
что демо-узел формально уровня L4 и проверка `valid_l4` его пропускает.
**Без заслона 421 задача уехала бы в `tasks_master` на фиктивный навык**, и
класс выглядел бы успешно оцифрованным.

### Чего в этих цифрах нет

* **`task_recall` и `latex_ned`** — нужен вручную вычитанный golden.
* **Полнота книги: 95.7%.** Прогон остановлен на хвосте §6: ключ упёрся в 429,
  за час прибавилось 8 задач. Извлечение прошло по всем 23 параграфам,
  недосчитаны дистракторы у части задач последнего раздела.
* **Страница 7 потеряна** в этом прогоне (упала на Vision-OCR до того, как был
  добавлен фолбэк на текстовый слой). Чинится точечным перепрогоном §1.2.
* **`compile_rate = 1.0` не означает «формулы верны»** — см. раздел про
  лексические артефакты выше. Здесь их ноль, но проверять нужно оба сигнала.

### Что этот прогон стоил

Двенадцать дефектов, **ни один из которых не виден на тестах и демо-прогоне**.
Три блокировали оцифровку полностью (B12, B13, B24), четыре молча теряли или
портили данные (B14, B17, B18, B20). Разбор — в `PROGRESS.md` и реестре §4
`CLAUDE.md`.

---

## Что означают метрики

| Метрика | Смысл | Появилась |
|---|---|---|
| `task_recall` | Доля golden-задач, найденных в банке. Матч по (параграф, номер) | С1 |
| `compile_rate` | Доля формул, которые компилируются KaTeX | С2 |
| `numbering_gaps` | Пропуски номеров упражнений внутри параграфа. Единственный сигнал, ловящий ошибки сегментации | С1 |
| `latex_ned` | Нормализованный edit distance по канонизованному LaTeX против golden. 0 = идеально | С1 |
| `answer_coverage` | Доля ответов, взятых **из книги**, а не сгенерированных ИИ | С3 |
| `needs_review` | Доля задач с **браком** распознавания — очередь ручной проверки | С5 |
| `awaiting_answer` | Доля задач без ответа из книги. Полнота, не качество | С5 |

BLEU не используется.
