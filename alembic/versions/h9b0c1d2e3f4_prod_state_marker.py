"""маркер состояния, в котором прод оказался мимо репозитория

Revision ID: h9b0c1d2e3f4
Revises: f8a9b0c1d2e3
Create Date: 2026-09-02

Эта ревизия ничего не делает — и в этом весь смысл.

`alembic_version` продовой базы (выгрузка 2026-09-01) содержит именно этот
идентификатор, `h9b0c1d2e3f4`, а файла с ним не было ни в одной ветке. Из-за
этого `alembic current` на проде падал с «Can't locate revision», а значит
падал бы и `alembic upgrade head` в автодеплое — независимо от того, что мы
пишем в коде.

Вариантов было два: штамповать прод руками (`UPDATE alembic_version …`) или
вернуть недостающее звено в цепочку. Второй лучше: ручной штамп — разовое
необратимое действие на живой базе, которое надо не забыть выполнить ровно
один раз и ровно там; звено в цепочке работает само и одинаково везде.

Работу, которая довела прод до этого состояния, выполняет предыдущая ревизия
`f8a9b0c1d2e3` — она выведена из самого дампа (снимок `information_schema`
прода против чистой базы), а не написана по памяти. Поэтому здесь `upgrade`
пуст: база, прошедшая `f8a9b0c1d2e3`, уже находится в состоянии, которое этот
маркер обозначает.

Что это даёт:
* на проде журнал резолвится и `upgrade head` доезжает до конца;
* на чистой базе цепочка проходит через то же состояние;
* ручных действий на живой базе не требуется вовсе.

Если недостающие файлы миграций всё-таки найдутся у того, кто их применял в
августе, эту пару (`f8a9b0c1d2e3` + маркер) следует заменить на настоящие —
здесь восстановлен результат, а не история изменений.
"""
from alembic import op  # noqa: F401
import sqlalchemy as sa  # noqa: F401

revision = "h9b0c1d2e3f4"
down_revision = "f8a9b0c1d2e3"
branch_labels = None
depends_on = None


def upgrade() -> None:
    """Пусто намеренно: состояние достигнуто ревизией `f8a9b0c1d2e3`."""


def downgrade() -> None:
    """Пусто намеренно: маркер не вносит изменений, откатывать нечего."""
