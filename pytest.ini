[pytest]
# Root-level test_toc*.py are ad-hoc operator scripts that make live OCR/API
# calls and depend on local textbook files.  Keep CI deterministic by
# collecting only the maintained automated suite.
testpaths = tests
